export enum api {
    flats = "/flats",
    cars = "/cars",
    baths = "/baths-and-saunas/",
    cottages = "/cottages",
    navigation = "/navigation",
    menuList = "/menu",
    posts = "/posts",
    contacts = "/contacts"
}
