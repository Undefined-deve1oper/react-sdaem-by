export type refType = {
    current?: any;
};

export type HandleChangeDataType = {
    name: string;
    value: string;
};

export type SearchEstateFormDataType = {
    city: string;
    rooms: string;
    price: number[];
};
