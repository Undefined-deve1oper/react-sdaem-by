export const validatorConfig = {
    entry: {
        isRequired: {
            message: "Email обязателен для заполнения"
        }
    },
    departure: {
        isRequired: {
            message: "Email обязателен для заполнения"
        }
    }
};
