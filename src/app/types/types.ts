export type refType = {
    current?: any;
};

export type handleChangeDataType = {
    name: string;
    value: string;
};

export type SearchEstateFormDataType = {
    city: string;
    rooms: string;
    from: string;
    to: string;
};
